# Frontend Python sidecar runtime dependencies (packaged app)
#
# Full bundled runtime profile for the sidecar.

# Vector storage and search
faiss-cpu>=1.7.4
aiosqlite>=0.19.0

# HTTP client for backend communication (for embeddings)
aiohttp>=3.9.0

# Additional utilities
numpy>=1.24.0,<2.0
platformdirs>=3.0.0
pathspec>=0.10.0

# Wake-word detection
openwakeword

# Computer control (mouse, keyboard, screenshot)
pyautogui>=0.9.54
pynput>=1.7.6

# System/clipboard/image
psutil>=5.9.0
pyperclip>=1.8.2
Pillow>=10.0.0
pypdf>=5.5.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Browser automation
playwright>=1.40.0
markdownify>=0.13.1

# Platform-specific window management
pywin32>=306; sys_platform == 'win32'
pyobjc-framework-Cocoa>=10.3; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.3; sys_platform == 'darwin'
