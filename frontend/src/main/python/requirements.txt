# Frontend Python Local Backend Dependencies

# Vector storage and search
faiss-cpu>=1.7.4
aiosqlite>=0.19.0

# HTTP client for backend communication (for embeddings)
aiohttp>=3.9.0

# Additional utilities
numpy>=1.24.0,<2.0

# User data directories
platformdirs>=3.0.0

# Wake-word detection
openwakeword

# Computer control (mouse, keyboard, screenshot)
pyautogui>=0.9.54
pynput>=1.7.6

# System information
psutil>=5.9.0

# Clipboard access
pyperclip>=1.8.2

# Image processing (for screenshots)
Pillow>=10.0.0
pypdf>=5.5.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Testing
pytest>=8.1.1
pytest-asyncio>=0.23.6

# Browser automation
playwright>=1.40.0
# Windie browser runtime dependencies
markdownify>=0.13.1

# Gitignore parsing
pathspec>=0.10.0

# Platform-specific window management
# Windows
pywin32>=306; sys_platform == 'win32'

# macOS
pyobjc-framework-Cocoa>=10.3; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.3; sys_platform == 'darwin'

# Linux: xdotool should be installed via system package manager
# sudo apt-get install xdotool  (Debian/Ubuntu)
# sudo yum install xdotool      (RHEL/CentOS)
